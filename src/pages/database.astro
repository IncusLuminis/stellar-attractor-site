---
// /database — system placeholder (#19 AC 2). Real Database Index + search is
// Phase 4. This is also the fallback link target from 404 and every entity page,
// so it must always exist.
import SystemPlaceholder from "../components/SystemPlaceholder.astro";
---

<SystemPlaceholder system="database" title="DATABASE INDEX" phase="Phase 4" />
