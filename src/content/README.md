# src/content/

Astro content collections + Zod schemas per entity type. In Astro 5+ the config
file is `src/content.config.ts` (repo root of this folder is kept for collection
assets). Implementation Plan §3, §4. Authored in #16.
