# src/hud/

SVG/CSS HUD component library (HudFrame, HudCorners, HudReticle, …) plus
`hud.css`, which imports the shared tokens via `src/styles/tokens.css`.
Implementation Plan §3, §6. Built in Phase 2 (#24–#28).
